import randomIntFromInterval from "./randomIntFromInterval";

const randomRGB = () => [randomIntFromInterval(0, 255), randomIntFromInterval(0, 255), randomIntFromInterval(0, 255)];

export default randomRGB;
